# Local development only. Copy to .env to override; .env is gitignored.
API_PORT=5035
POSTGRES_PORT=55432
POSTGRES_DB=startpack
POSTGRES_USER=startpack
POSTGRES_PASSWORD=local-development-only
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025
