{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "//": "Local development only. Real credentials come from the environment (§25) and never from a committed file. `Include Error Detail` puts parameter VALUES into Npgsql exception messages — invaluable locally, a data leak in production, which is why it lives here and not in appsettings.json.",
    "Postgres": "Host=localhost;Port=5432;Database=authapi;Username=authapi;Password=authapi;Include Error Detail=true"
  }
}
